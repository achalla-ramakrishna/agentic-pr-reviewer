package com.codewalnut.prreviewer.domain;

/**
 * Not Java-only by design (see docs/architecture.md, "Extending beyond
 * Java") even though JAVA is the only language with seeded practices today.
 */
public enum Language {
    JAVA,
    TYPESCRIPT,
    JAVASCRIPT,
    GENERAL
}
